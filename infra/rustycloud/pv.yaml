apiVersion: v1
kind: PersistentVolume

metadata:
  name: rustycloud-pv

spec:
  capacity:
    storage: 10Gi

  accessModes:
    - ReadWriteMany

  persistentVolumeReclaimPolicy: Retain

  nfs:
    server: ${NFS_SERVER}
    path: ${NFS_MOUNT_POINT}