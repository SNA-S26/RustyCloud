apiVersion: v1
kind: Secret

metadata:
  name: rustycloud-secret

type: Opaque

stringData:
  MONGODB_URI: ${MONGODB_URI}
  REDIS_URI: ${REDIS_URI}
  NFS_MOUNT_POINT: ${NFS_MOUNT_POINT}
  NFS_SERVER: ${NFS_SERVER}